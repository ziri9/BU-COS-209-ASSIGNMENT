export function add(a, b) { return a + b; }
export const PI = 3.14159;

//Allow code to be split into separate files for better organization and reusability.

//Use export (named or default) to share, and import to bring code into another file.